{
  // 使用 IntelliSense 了解相关属性。
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run MCP Dev Server (Terminal)",
      "type": "node-terminal", // 使用 node-terminal 类型
      "request": "launch",
      "command": "uv run mcp dev server.py", // 要执行的命令
      "cwd": "${workspaceFolder}" // 工作目录
    }
  ]
}
